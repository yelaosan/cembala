<view class="container">
    <block wx:for="{{musics}}" wx:key="id" wx:for-index="idx">
      <view class="musics-swiper-item">
        <import src="templates/musicItem.wxml"/>
        <template is="music-item" data="{{item: item}}"/>
      </view>
    </block>
</view>
